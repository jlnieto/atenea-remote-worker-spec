Every task requires complete tests, documentation, strict validation, commit
and publication before the next task.

## 0. Entry and exact profiles

- [ ] 0.1 Audit current verifier, project scripts, AX42 slots/toolchains and
  accepted Atenea source in read-only mode; define closed validation profiles,
  digests, limits, network and no-impact baseline

## 1. Domain and persistence

- [ ] 1.1 Add red tests and expand-only schema for plans, runs, checks,
  leases, progress, receipts and stale projection
- [ ] 1.2 Implement disabled plan/create/read/cancel/reconcile APIs with exact
  source/policy/definition fingerprints and failure taxonomy

## 2. Protected AX42 broker

- [ ] 2.1 Add typed broker protocol and fixed mediators for backend, web,
  Android and Playwright; prove no client/repo shell, path, image, slot, port or
  credential authority
- [ ] 2.2 Add rootless admission, resource/time/network limits, artifact
  finalization and terminal receipts; pass focused isolation/security tests
- [ ] 2.3 Prove backend restart, worker restart, partition, response loss,
  cancellation, full capacity and exact receipt idempotence

## 3. First vertical canary

- [ ] 3.1 Run complete synthetic validation/capacity suites and seal an exact
  backend/worker/toolchain rollout; stop for H1/H2/H3
- [ ] 3.2 After authorization, install/activate only shadow/synthetic Atenea
  validation with no AgentRun or WorkSession 19 use
- [ ] 3.3 After a separate exact canary authorization, run one Atenea
  `ANDROID_BUILD` plus required non-production checks; retain artifacts and
  stop before APK publication, preview, integration or release
- [ ] 3.4 Observe, prove source-stale invalidation and no impact, then archive
